# Ponytail Skill

Enforces the laziest solution that actually works: a decision ladder that checks
necessity, existing code, stdlib, native features, and dependencies before any
custom code gets written.

## Source

Vendored from DietrichGebert/ponytail:

**Original Source:** <https://github.com/DietrichGebert/ponytail/blob/main/skills/ponytail/SKILL.md>

License: MIT. Snapshot date: 2026-08-24.

The upstream frontmatter also includes `argument-hint` and `license` keys; those are
dropped here to match this repo's convention (`name` + `description` only). The
intensity-level argument is documented in the body instead.

## Updating

No sync script — this is a single small, stable file. To refresh, re-fetch the
upstream `SKILL.md`, diff by hand, update this repo's `SKILL.md`, and run `hms`.
