#!/bin/bash
pgrep gpg-agent | xargs kill -9; /usr/local/MacGPG2/bin/gpgconf --launch gpg-agent
if [ -f "${HOME}/.gpg-agent-info" ]; then
     . "${HOME}/.gpg-agent-info"
       export GPG_AGENT_INFO
       export SSH_AUTH_SOCK
       export SSH_AGENT_PID
fi
