#!/bin/bash

# check for card status before killing gpg-agent
gpg --card-status >/dev/null 2>&1 || {
    echo "killing gpg-agent"
    kill -9 $(pgrep gpg-agent)
}

# now reload stuff from .profile (assuming this is where gpg stuff is)
[ -f "${HOME}/.profile" ] && . "${HOME}/.profile"
